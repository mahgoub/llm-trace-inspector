# v0.3.1 Release Notes

Marketplace readiness update:

- Add GitHub Marketplace branding metadata to `action.yml`.
- Rename the Action to `LLM Trace Inspector RAG Eval` for clearer Marketplace discovery.
- Remove repository workflow files so the repo satisfies GitHub Marketplace Action listing requirements.
- Update README Action usage to `mahgoub/llm-trace-inspector@v0.3.1`.

